import '../../models/exam_package.dart';

class EntrepreneurshipPackages {
  /// Category exists but currently has 0 packages.
  /// The app UI handles empty state gracefully without errors.
  static final List<ExamPackage> list = [];
}
